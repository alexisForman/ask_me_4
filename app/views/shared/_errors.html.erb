<% if form.object.errors.any? %>
    <div style="color: red">
        <h2>
            <%= pluralize(form.object.errors.count, "error") %> prohibited your user profile from being created:
        </h2>

        <ul>
            <% form.object.errors.full_messages.each do |error| %>
        <li>
            <%= error.full_message %>
        </li>
        <% end %>
        </ul>
    </div>
<% end %>